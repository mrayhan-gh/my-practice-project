import React from 'react';
import Header from '../Header/Header';

const DarkMode = () => {
    return (
        <div>
            <Header></Header>
            <h1>This is our dark mode page</h1>
        </div>
    );
};

export default DarkMode;