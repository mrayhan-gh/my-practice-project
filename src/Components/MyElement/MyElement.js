import React from 'react';
import Header from '../Header/Header';

const MyElement = () => {
    return (
        <div>
            <Header></Header>
        </div>
    );
};

export default MyElement;